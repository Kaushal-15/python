import numpy as np
a=np.array([1,24,45])
b=np.array([[1,2,3,4,],[22,44,53,51]])
#to check the dimension of the array
print(b.shape)
print(a.shape)
#to check the size that is the total elements of the array
print(a.size)
print(b.size)
